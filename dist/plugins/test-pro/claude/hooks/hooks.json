{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
<<<<<<< HEAD
            "command": "sh -c 'AIDLC=$(command -v aidlc 2>/dev/null || true); [ -n \"$AIDLC\" ] && { AIDLC_HARNESS_DIR=.claude AIDLC_HARNESS_NAME=claude \"$AIDLC\" plugin sync && exit 0; }; BUN=$(command -v bun 2>/dev/null || true); [ -z \"$BUN\" ] && [ -x \"$HOME/.bun/bin/bun\" ] && BUN=\"$HOME/.bun/bin/bun\"; [ -z \"$BUN\" ] && { echo \"aidlc plugin compose: aidlc and bun not found, skipping\" >&2; exit 0; }; AIDLC_HARNESS_DIR=.claude AIDLC_HARNESS_NAME=claude \"$BUN\" \"${CLAUDE_PLUGIN_ROOT}/hooks/compose.ts\"'",
||||||| parent of 1be171311 (feat: add native plugin state synchronization)
            "command": "sh -c 'AIDLC=$(command -v aidlc 2>/dev/null || true); [ -n \"$AIDLC\" ] && { AIDLC_HARNESS_DIR=.claude \"$AIDLC\" plugin sync && exit 0; }; BUN=$(command -v bun 2>/dev/null || true); [ -z \"$BUN\" ] && [ -x \"$HOME/.bun/bin/bun\" ] && BUN=\"$HOME/.bun/bin/bun\"; [ -z \"$BUN\" ] && { echo \"aidlc plugin compose: aidlc and bun not found, skipping\" >&2; exit 0; }; AIDLC_HARNESS_DIR=.claude \"$BUN\" \"${CLAUDE_PLUGIN_ROOT}/hooks/compose.ts\"'",
=======
            "command": "sh -c 'AIDLC=$(command -v aidlc 2>/dev/null || true); [ -n \"$AIDLC\" ] && { AIDLC_HARNESS_DIR=.claude \"$AIDLC\" plugin sync && exit 0; }; BUN=$(command -v bun 2>/dev/null || true); [ -z \"$BUN\" ] && [ -x \"$HOME/.bun/bin/bun\" ] && BUN=\"$HOME/.bun/bin/bun\"; [ -z \"$BUN\" ] && { echo \"aidlc plugin compose: aidlc and bun not found, skipping\" >&2; exit 0; }; PROJECT_ROOT=\"${CLAUDE_PROJECT_DIR:-${AIDLC_PROJECT_DIR:-$PWD}}\"; PLUGIN_TOOL=\"$PROJECT_ROOT/.claude/tools/aidlc-plugin.ts\"; [ -f \"$PLUGIN_TOOL\" ] && { AIDLC_HARNESS_DIR=.claude \"$BUN\" \"$PLUGIN_TOOL\" sync && exit 0; }; AIDLC_HARNESS_DIR=.claude \"$BUN\" \"${CLAUDE_PLUGIN_ROOT}/hooks/compose.ts\"'",
>>>>>>> 1be171311 (feat: add native plugin state synchronization)
            "statusMessage": "AIDLC test-pro: composing plugin"
          }
        ]
      }
    ]
  }
}
